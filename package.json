{
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "debug": "npx wrangler pages dev --local-protocol=https dist"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240129.0",
    "esbuild": "^0.20.0",
    "glob": "^10.3.10",
    "wrangler": "^3.26.0"
  }
}
