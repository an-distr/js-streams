{
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "test-web": "npx wrangler pages dev --local-protocol=https dist",
    "test-node": "node --experimental-default-type=module test_runner.js",
    "test-deno": "deno run --allow-all test_runner.ts",
    "test-bun": "bun test_runner.ts"
  },
  "devDependencies": {
    "@clack/prompts": "^0.7.0",
    "@cloudflare/workers-types": "^4.20240129.0",
    "esbuild": "^0.20.0",
    "fast-glob": "^3.3.2",
    "wrangler": "^3.26.0"
  }
}
